<!DOCTYPE html> 
<html>   
  <head>     
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />   
  </head>   
  <body>
<i>
Hello,
</i>
<br/>
<p>
<%= @user.email %> have applyed for leaves.
<br>
reason:
</br>
      <p><%= @leave.reason %>
      <br />
from date:<br/>       <%= @leave.starts_at.strftime("%A, %B %d, %Y") %><br/> to  <br/> <%= @leave.ends_at.strftime("%A, %B %d, %Y") %></p>
<p>You can Approve it by clicking on this link:   <%= link_to 'click hear', leaves_path(only_path: false,       host: [@user.organization.slug, '.',"niwantintranet.com"].join) %> to take action.
    <br />
    <p>Thanks and Regards,<br />
  <%= @user.organization.name %> Team</p>   
  </body> 
</html> 


