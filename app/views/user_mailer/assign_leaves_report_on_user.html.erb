<!DOCTYPE html> 
<html>   
  <head>     
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />   
  </head>   
  <body>
<i>
  Hello <%= @user.profile.nil? ? @user.email.split("@").first : @user.name %>
</i>
<br/>
<p>as a [<%= @user.roles %>] of [<%= @user.organization.name %>], you have been assigned leave for the year[<%= Date.today.year %>].
Please check the assigned leaves details by clicking on link below.<br>
<%= link_to "Click heare", leaves_path(only_path: false, host: [@user.organization.slug, '.',"niwantintranet.com"].join)
<br>
<br>
<b>Warm Regards<br>
<%= @user.organization.name.capitalize %> team</b>
  
  </body> 
</html> 

