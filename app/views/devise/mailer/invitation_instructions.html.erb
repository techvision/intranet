<p>Hello <%= @resource.email %>!</p>

<p><%= @resource.organization.name %> invited you to: </br> 
<%= link_to root_url( host: [@resource.organization.slug, '.',JoshIntranet::Application.config.action_mailer.default_url_options[:host]].join), root_url( host: [@resource.organization.slug, '.',JoshIntranet::Application.config.action_mailer.default_url_options[:host]].join) %></br> This web portal will manage your all leaves automatically.
<br/>
Only  you need to   accept an invitation from belows link.
If you want to accept an invitation, you can accept it through the link below.</p>

<p><%= link_to 'Accept invitation', accept_invitation_path(@resource, :invitation_token => @resource.invitation_token, only_path: false, 
host: [@resource.organization.slug, '.',JoshIntranet::Application.config.action_mailer.default_url_options[:host]].join) %></p>

<p>But if you don't want to accept the invitation, please ignore this email.<br />
Note: <br/> Your account won't be created until you access the link above and set your password.</p>

<p>Thanks & regards </br>
<%= @resource.organization.name %> team.</p>   

 
